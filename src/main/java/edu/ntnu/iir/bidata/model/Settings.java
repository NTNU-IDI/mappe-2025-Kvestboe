package edu.ntnu.iir.bidata.model;

public class Settings {
    // handles the settings of the user, so that every user can have a different configuration
    // communicates with the settings.json file
    // loads the settings upon start
    // password encryption if included in program
}
